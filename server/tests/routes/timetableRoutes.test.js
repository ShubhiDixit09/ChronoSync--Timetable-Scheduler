// Placeholder integration test — wire up supertest + an in-memory Mongo instance
// (e.g. mongodb-memory-server) before filling this in.
describe('timetable routes', () => {
  it.todo('POST /api/timetable/generate enqueues a generation job');
  it.todo('GET /api/timetable/:jobId/status returns job progress');
  it.todo('POST /api/timetable/:id/approve requires admin role');
});
